<IfModule mod_rewrite.c>
    RewriteEngine On
    RewriteBase /

    # Redirect HTTP to HTTPS for web browser traffic (excluding APIs)
    RewriteCond %{HTTPS} off
    RewriteCond %{REQUEST_URI} !^/(api|backend|index\.php) [NC]
    RewriteRule ^ https://%{HTTP_HOST}%{REQUEST_URI} [L,R=301]

    # Exclude backend API and PHP routes from React SPA fallback
    RewriteCond %{REQUEST_URI} ^/(backend|api|index\.php) [NC]
    RewriteRule ^ - [L]

    RewriteCond %{REQUEST_FILENAME} !-f
    RewriteCond %{REQUEST_FILENAME} !-d
    RewriteRule . /index.html [L]
</IfModule>